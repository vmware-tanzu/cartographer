apiVersion: kpack.io/v1alpha2
kind: Image
metadata:
  name: my-workload-name
spec:
  tag: some-default-prefix-my-workload-name
  serviceAccountName: cartographer-example-registry-creds-sa
  builder:
    kind: ClusterBuilder
    name: go-builder
  source:
    blob:
      url: some-passed-on-url
