package performance_test

import (
	. "github.com/onsi/ginkgo/v2"
)

func MakeWorkloads(numWorkloads int64) error {
	//workloads need to have runnables
}

var _ = Describe("Benchmark", func() {
	It("has some timings...", func() {
		//
		//make n workloads
		//wait for completion
		//
	})
})
