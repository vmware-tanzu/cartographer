---
commentWrapAt: 80
version: v1alpha1
fields:
  spec:
    fields:
      build:
        fields:
          env:
            # really should be in the docs
            appendDescription: See https://kubernetes.io/docs/tasks/inject-data-application/environment-variable-expose-pod-information/
            hideChildDescriptions: true
            maxDepth: 1
      env:
        # really should be in the docs
        appendDescription: See https://kubernetes.io/docs/tasks/inject-data-application/environment-variable-expose-pod-information/
        hideChildDescriptions: true
        maxDepth: 1
      resources:
        hideChildDescriptions: true
  status:
    hide: true
